# Optional: without both values, recommendations use deterministic explanations.
# API model verified by the live Demo A smoke test on 2026-09-23.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Test runner / smoke options: set these in the shell, not in the Next.js env file.
# BASE_URL=http://localhost:3000
# EXPECT_FALLBACK=1
# SMOKE_AI_CALLS_USED=0
# INVARIANT_DATE_STEP=2
